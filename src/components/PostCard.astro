---
// 受け取るprops：記事情報
const { title, date, image, excerpt, href } = Astro.props;
---

<article class="bg-white rounded-xl shadow hover:shadow-md transition overflow-hidden">
  <a href={href} class="block">
    <img src={image} alt={title} class="w-full h-48 object-cover" loading="lazy" />
    <div class="p-4">
      <time class="block text-xs text-gray-500 mb-1">{date}</time>
      <h2 class="text-lg font-semibold text-green-900">{title}</h2>
      <p class="mt-2 text-sm text-gray-700">{excerpt}</p>
    </div>
  </a>
</article>
