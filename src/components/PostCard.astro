---
const { title, summary, image, date, slug } = Astro.props;
---

<article class="flex flex-col md:flex-row md:items-center gap-4 border rounded-lg shadow bg-white overflow-hidden hover:shadow-lg transition">
  <div class="flex-1 p-4">
    <div class="text-xs text-gray-500 mb-1">{date}</div>
    <h2 class="text-lg font-bold mb-2">
      <a href={`/posts/${slug}`} class="hover:text-green-700 transition">{title}</a>
    </h2>
    <p class="text-sm text-gray-700 line-clamp-3">{summary}</p>
  </div>
  <img
    src={image}
    alt={title}
    class="w-full md:w-44 h-32 object-cover"
    loading="lazy"
  />
</article>
